---
description: Explain existing automated tests and assertions without editing code.
argument-hint: <file|ATC|ticket|module>
---

Invoke skill `test-automation` in mode `explain`.
Forward `$ARGUMENTS` unchanged.
