---
description: Adapt KATA and repository tooling after an approved no-write analysis and plan.
argument-hint: [entity-or-scope]
---

Invoke skill `adapt-framework` in mode `adapt`.
Forward `$ARGUMENTS` unchanged.
